# Requisitos del backend principal (backend/) — SIN TensorFlow a propósito.
# TensorFlow vive aislado en ml_service/ (ver requirements-ml.txt), separado
# de este proceso porque TensorFlow + un driver de Postgres real en el mismo
# proceso causaba un Segmentation fault reproducible en Render.
#
# Requiere Python 3.10 u 3.11
fastapi==0.104.1
uvicorn[standard]==0.24.0
httpx==0.27.0
numpy==1.26.4
python-multipart==0.0.6
pydantic==2.5.3
python-dotenv==1.0.0

# Autenticación y base de datos (usuarios + historial real)
sqlalchemy==2.0.35
bcrypt==4.2.0
PyJWT==2.9.0
email-validator==2.2.0
psycopg[binary]==3.2.3